# Popup nạp Point — 24/09/2026

User approved dark-green wallet card. Implemented cream package chooser, original PointCoin, separate checkout summary, native modal, mobile bottom sheet, reduced-motion styles. No third-party inspiration/template used; existing Next.js16.3.5 and AstroX identity.

Selection does not create an order. Explicit checkout uses server package amount. Request lock prevents duplicate clicks; network failure releases pending state. Edited promo invalidates approved bonus; stale promo response ignored. Closing/unmounting ignores delayed checkout redirect. Guest and localhost-preview accounts cannot submit payment. Package list, account scope and promo selection reset on reopen/account switch.

## Validation

- Final production build: passed, temporary `/qa-topup-local` harness removed before build and absent from export.
- ESLint changed TSX: passed.
- Existing frontend suite plus new behavior tests: 29/29 passed; final focused tests: 6/6 passed.
- New tests use a controlled hook/API harness to exercise actual component handlers, not live payment or full React DOM integration.
- CUA browser: desktop1440x1000, mobile390x844 and360x740; scoped dialog fits viewport, no horizontal overflow, selected55Point/50,000VND summary correct, promo disclosure opens, payment footer stays within screen (button bottom683.2px on740px viewport); Escape closes popup (dialog count0).
- Public package API snapshot used in temporary local fixture because local cross-origin request failed. Prices remain server-owned and unchanged. Preview stays payment-disabled; no authenticated live payment/promo redemption was performed.
- Error state inspected in actual browser before using fixture; authenticated error/concurrency/promo cases verified with mocks.
- Backend, payment provider config and rewards untouched.
