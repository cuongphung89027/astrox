# Release note — Nâng cấp Chỉ tay (palm capture upgrade)

- **Ngày:** 25/09/2026
- **Merge commit:** `9606e11` (branch `codex/palm-upgrade`, 16 commits, base `a009880`; tip đã QA `e1e0567`)
- **Phạm vi:** chỉ frontend static export (Pages). Worker/DB/Admin không đụng — không migration.
- **Remotes:** pushed `a009880..9606e11` → origin (ngthson553-create/astrox) + upstream (cuongphung89027/astrox); Pages theastrox build từ upstream.

## Nội dung

- `/chitay` luồng mới: hướng dẫn chụp đúng/sai → camera live nhận diện bàn tay MediaPipe on-device (model+wasm self-host, lazy load, timeout 4s → chụp thủ công) → dẫn khung + tự chụp 3-2-1 → chọn lens wide máy multi-cam (vá lỗi cam tele Find X9 Ultra) + nút đổi lens → cảnh báo sáng/tối (không chặn) → reveal overlay vẽ dần từng đường + chấm đầu ngón.
- Không đổi: prompt AI (`palm.read.v1`), schema, giá/badge, luồng charge, consent, Worker.
- Assets mới trên Pages: `/models/hand_landmarker.task` (7.8 MB), `/mediapipe/wasm/` (6 file, ~34 MB).

## Checks sau deploy (25/09)

- `https://theastrox.space/chitay` → 200, marker "Chỉ tay — AstroX" ✓
- `/models/hand_landmarker.task` → 200, magic `PK`, size 7,819,105 byte = khớp repo byte-exact ✓
- `/mediapipe/wasm/vision_wasm_internal.js` → 200, 322,044 byte = khớp local ✓
- `/tarot` → 200; `/api/site-config` → 200 (không regression) ✓
- QA trước merge: test 323/323 + 79 prompt, typecheck 0, lint 0 error, build 22/22, /chitay×30 = 0 lỗi, ui-30viewports 518/518, engine webkit+chromium 6/6.

## Rollback

Revert deploy Pages về build trước (revision liền trước trên Cloudflare Pages) — không cần rollback Worker/DB.

## Điều kiện nghiệm thu cuối (chưa xong — cần máy thật)

Xem `qa-report/palm/device-matrix.md`: Find X9 Ultra (đối chứng lens wide), **iPhone Safari (chưa từng test camera)**, Android phổ thông, desktop. Mọi nhánh lỗi có fallback (model fail → chụp thủ công; lens fail → mở lại mặc định; camera fatal → upload ảnh), nên rủi ro là trải nghiệm chứ không phải outage.

## Follow-up 25/09 (sau nghiệm thu máy thật) — merge `1ad91c0`, branch codex/palm-fix-2

- Fix 1 lens: luôn dò khi ≥2 cam sau (bỏ gate zoom-null), probe zoom từ track caps + ImageCapture, loại lens theo nhãn (tele/ultra/macro…), lọc cam trước, **nhớ lựa chọn vào localStorage `astrox.palm.lensId`** (user đổi 1 lần, tự đúng mãi), sync lensIdx với lens đang mở, log `[palm] lens scan` để debug máy thật.
- Fix 2: bàn tay **5 ngón** (path `PALM_HAND_PATH` dùng chung), popup hướng dẫn **trước khi chụp** (1 lần/phiên, dialog a11y, trả focus).
- Fix 3: **bỏ timeout 4s**, màn loading riêng (bàn tay vàng vẽ dần + copy 2 nhịp, fade khi model sẵn sàng, lỗi thật mới rơi chụp thủ công), nút Chụp ảnh khóa khi loader.
- Prod verify: bundle mới có `palmGuideSeen` + path 5 ngón; `/chitay` 200; model/wasm 200.
- **Lưu ý cache:** `_headers` immutable KHÔNG được Pages project hiện tại đọc (trả `max-age=0, must-revalidate`) — nhưng ETag + 304 hoạt động: browser giữ model trong disk cache, lần sau không tải lại 8 MB. Muốn immutable tuyệt đối thì cấu hình Cache Rules trên dashboard Cloudflare Pages. Bump `@mediapipe/tasks-vision` về sau cần đổi đường dẫn version để bust cache.
- QA: test 344/344, lint 0 error, typecheck 0, build 22/22, /chitay×30 = 0 lỗi, ui-30viewports 518/518, engine 6/6, fake-camera smoke 29/29 + probe popup/scroll/focus.
