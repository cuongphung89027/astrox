import test from 'node:test';import assert from 'node:assert/strict';
import {testEnv} from './test/sqlite.mjs';import {defaultConfig} from './config.ts';import {state,publish,readPublished} from './store.mjs';import {handlePublic,handleConfiguredAi,handleAdminRuntime} from './integration-api.mjs';import {providerHealth} from './health-store.mjs';
const request=(path,body)=>new Request('https://theastrox.space'+path,{method:body?'POST':'GET',...(body?{body:JSON.stringify(body),headers:{'content-type':'application/json'}}:{})});
test('unpublished AI leaves legacy path intact and public config does not disclose drafts',async()=>{const env=testEnv();await state(env);assert.equal(await handleConfiguredAi(request('/api/ai',{}),env),null);assert.equal((await(await handlePublic(request('/api/site-config'),env)).json()).config,null)});
test('public config projection exposes only published version and no secrets',async()=>{const env=testEnv();await state(env);const c=defaultConfig();c.content.announcement='published';c.integrations.payos.clientId='private';await publish(env,'owner',c,0,'publish');const r=await handlePublic(request('/api/site-config'),env);const body=await r.json();assert.equal(body.config.content.announcement,'published');assert.equal(body.config.integrations,undefined);assert.equal(body.revision,(await readPublished(env)).revision)});
test('published disabled/draft AI cannot fall back to hard-coded provider',async()=>{const env=testEnv();await state(env);await publish(env,'owner',defaultConfig(),0,'publish');const res=await handleConfiguredAi(request('/api/ai',{serviceId:'tuvi',messages:[{role:'user',content:'test'}]}),env);assert.equal(res.status,503)});
test('paid AI delegates to authoritative backend and does not run public provider directly',async()=>{const env=testEnv();await state(env);const c=defaultConfig();c.ai.enabled=true;c.billing.enabled=true;await publish(env,'owner',c,0,'publish');assert.equal((await handleConfiguredAi(request('/api/ai',{operationId:'same',serviceId:'tuvi',messages:[{role:'user',content:'test'}]}),env)).status,503);let sent;env.ASTROX_BACKEND={fetch:async r=>{sent={url:r.url,body:await r.json()};return Response.json({owned:true})}};assert.equal((await(await handleConfiguredAi(request('/api/ai',{operationId:'same',serviceId:'tuvi',messages:[{role:'user',content:'test'}]}),env)).json()).owned,true);assert.equal(sent.body.operationId,'same');assert.ok(sent.url.endsWith('/internal/ai'))});
test('provider health is persisted and reset across separate handlers',async()=>{const env=testEnv();await providerHealth(env).recordFailure('a',100);await providerHealth(env).recordFailure('a',200);assert.deepEqual(await providerHealth(env).get('a'),{failures:2,lastFailureAt:200});await providerHealth(env).recordSuccess('a');assert.equal(await providerHealth(env).get('a'),null)});
test('integration readiness never pretends to validate real OAuth or payment',async()=>{const env=testEnv();await state(env);const res=await handleAdminRuntime('integrations/zalo',request('/api/admin/integrations/zalo',{}),env,{email:'owner',capabilities:['secrets.write']});const data=await res.json();assert.equal(data.ok,false);assert.equal(data.scope,'configuration-only');assert.ok(data.checks.some(c=>!c.ok))});
test('AI disable cannot be bypassed by enabling billing',async()=>{const env=testEnv();await state(env);const c=defaultConfig();c.billing.enabled=true;await publish(env,'owner',c,0,'publish');let called=false;env.ASTROX_BACKEND={fetch:async()=>{called=true;return Response.json({})}};const r=await handleConfiguredAi(request('/api/ai',{operationId:'op',serviceId:'tuvi',messages:[{role:'user',content:'test'}]}),env);assert.equal(r.status,503);assert.equal(called,false)});
test('configured API rejects images and malformed messages before any backend handoff',async()=>{const env=testEnv();await state(env);const c=defaultConfig();c.ai.enabled=true;c.billing.enabled=true;await publish(env,'owner',c,0,'publish');let called=false;env.ASTROX_BACKEND={fetch:async()=>{called=true;return Response.json({})}};for(const messages of [null,{},[null],[{role:'user',content:[{type:'image_url',image_url:{url:'https://example.com/image.png'}}]}]]){const r=await handleConfiguredAi(request('/api/ai',{serviceId:'tuvi',messages}),env);assert.equal(r.status,400)}assert.equal(called,false)});
test('authority handoff strips spoofed headers and includes published revision',async()=>{const env=testEnv();await state(env);const c=defaultConfig();c.ai.enabled=true;c.billing.enabled=true;await publish(env,'owner',c,0,'publish');let captured;env.ASTROX_BACKEND={fetch:async r=>{captured=r;return Response.json({})}};const r=new Request('https://theastrox.space/api/ai',{method:'POST',headers:{'content-type':'application/json','x-admin-actor':'fake','x-internal-authorized':'yes',cookie:'astrox_session=user'},body:JSON.stringify({operationId:'same',serviceId:'tuvi',messages:[{role:'user',content:'test'}]})});await handleConfiguredAi(r,env);assert.equal(captured.headers.get('x-admin-actor'),null);assert.equal(captured.headers.get('x-internal-authorized'),null);assert.equal(captured.headers.get('x-astrox-config-revision'),'1');assert.equal(captured.headers.get('cookie'),'astrox_session=user')});
test('successful provider call persists sanitized token/cache usage and timing for metrics',async()=>{const {saveSecret}=await import('./store.mjs');const env=testEnv();env.PROVIDER_ALLOWED_HOSTS='api.example.com';await state(env);const c=defaultConfig();c.ai.enabled=true;c.ai.chain=['gateway'];c.billing.services[0].status='free';c.ai.providers=[{id:'gateway',name:'Gateway',model:'test-model',protocol:'chat',baseUrl:'https://api.example.com/v1',enabled:true,timeoutMs:1000,retries:0,maxTokens:100,temperature:0.5,secretRef:'provider:gateway',pricing:{input:2,output:10,cacheRead:0.2,cacheWrite:0}}];await saveSecret(env,'owner','provider:gateway','secret-never-log');await publish(env,'owner',c,0,'metrics test');const original=globalThis.fetch;globalThis.fetch=async()=>Response.json({choices:[{message:{content:'private response'},finish_reason:'stop'}],usage:{prompt_tokens:100,completion_tokens:20,prompt_tokens_details:{cached_tokens:60}}});try{const response=await handleConfiguredAi(request('/api/ai',{serviceId:'tuvi',messages:[{role:'user',content:'private prompt'}]}),env);assert.equal(response.status,200);const row=await env.DB.prepare('SELECT * FROM admin_ai_requests').first();const attempt=JSON.parse(row.attempts)[0];assert.equal(attempt.model,'test-model');assert.equal(attempt.usage.input,100);assert.equal(attempt.usage.cacheRead,60);assert.ok(attempt.durationMs>=0);assert.ok(attempt.costUsd>0);assert.ok(!JSON.stringify(row).includes('private'));assert.ok(!JSON.stringify(row).includes('secret-never-log'));}finally{globalThis.fetch=original}});
