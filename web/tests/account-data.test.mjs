import test from 'node:test';import assert from 'node:assert/strict';import {load,memoryStorage} from './support/load.mjs';
async function fixture(){const store=await load('lib/state.ts',{globals:{window:{},localStorage:memoryStorage()}});store.getState();return {store};}
test('first sign-in adopts guest data once; another account and logout cannot see it',async()=>{const {store}=await fixture();store.setState({profile:{name:'An'}});store.activateAccount('zalo:a');assert.equal(store.getState().profile.name,'An');store.writeAiCache('compatibility','pair','private');store.activateAccount('zalo:b');assert.equal(store.getState().profile,null);assert.equal(store.readAiCache('compatibility','pair'),'');store.activateAccount('zalo:a');assert.equal(store.readAiCache('compatibility','pair'),'private');store.activateAccount(null);assert.equal(store.getState().profile,null);});
